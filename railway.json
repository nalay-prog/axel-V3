{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "deploy": {
    "startCommand": "python -m flask --app backend.routes.api run --host 0.0.0.0 --port $PORT"
  }
}